{
  "name": "pi-ask-agi",
  "version": "1.0.0",
  "description": "Human-as-API for frontier models — compile prompts, deliver via Telegram, inject replies back into Pi",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/sshkeda/pi-ask-agi.git"
  },
  "pi": {
    "extensions": [
      "./extensions/pi-ask-agi/index.ts"
    ]
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*",
    "@sinclair/typebox": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.2"
  }
}
